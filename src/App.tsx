import Wizard from './components/wizard/Wizard';

/**
 * Root application component.
 * Renders the single-page onboarding wizard — no routing required.
 */
function App() {
  return <Wizard />;
}

export default App;
